/* Named configuration values for the mole appearance cycle. */

export const HOLE_COUNT = 9;

/* How long a mole stays visible, in milliseconds. */
export const MOLE_VISIBLE_MIN_MS = 650;
export const MOLE_VISIBLE_MAX_MS = 1200;

/* How long the board stays empty between moles, in milliseconds. */
export const MOLE_GAP_MIN_MS = 300;
export const MOLE_GAP_MAX_MS = 800;
