/* Application entry point. Module scripts are deferred, so the interface is
   ready to initialize as soon as this module runs. */

import { initializeInterface } from "./ui.js";

initializeInterface();
